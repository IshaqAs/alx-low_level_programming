#include <stdio.h>
/**
 *main - assign a random number to variable n each time it is
 *executed and print the last digit
 *Return: 0
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}

	print("\n");

	return (0);
}
